[project]
name = "games-intel-adapters-stt"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "games-intel-contracts",
    "games-intel-settings",
    "httpx>=0.27",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/games_intel"]
