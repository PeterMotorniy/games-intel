[project]
name = "games-intel-kafka"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "aiokafka>=0.11",
    "games-intel-contracts",
    "games-intel-db",
    "games-intel-settings",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/games_intel"]
