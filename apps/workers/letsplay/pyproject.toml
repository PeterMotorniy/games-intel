[project]
name = "games-intel-letsplay"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "games-intel-adapters-stt",
    "games-intel-adapters-youtube",
    "games-intel-agents-letsplay-analyst",
    "games-intel-agents-transcription",
    "games-intel-contracts",
    "games-intel-db",
    "games-intel-kafka",
    "games-intel-settings",
    "langchain>=1.4.0",
    "langgraph>=1.2.11",
    "langgraph-checkpoint-postgres>=3.1.2",
]

[project.scripts]
games-intel-letsplay = "games_intel.workers.letsplay.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/games_intel"]
