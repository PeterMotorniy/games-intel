# Соответствует reverse-proxy к Query API (`web.api_base_url` в settings).
# В dev Vite проксирует `/api` на http://localhost:8000.
VITE_API_BASE_URL=/api/v1
# Optional. Must match GAMES_INTEL__API__COMMAND_TOKEN when that is set.
VITE_API_COMMAND_TOKEN=
